import { LinktargetPipe } from './linktarget.pipe';

describe('LinktargetPipe', () => {
  it('create an instance', () => {
    const pipe = new LinktargetPipe();
    expect(pipe).toBeTruthy();
  });
});
